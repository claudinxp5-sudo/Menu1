-- BRASIL MENU - PAINEL DE DESENVOLVEDOR
-- Desenvolvido para testes e desenvolvimento em projetos privados

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera or game.Workspace:WaitForChild("Camera")

-- Variáveis de Estado
local isFlying = false
local flySpeed = 20 -- Velocidade inicial do fly
local currentWalkSpeed = 16 -- Velocidade de caminhada padrão
local isNoClip = false
local isInfiniteJump = false
local isSprint = false
local isAutoRun = false
local isFullBright = false
local isShowingNames = false
local isNoFog = false
local currentFOV = 70
local isZoomFree = false
local isEspEnabled = false
local isEspLineEnabled = false
local isEspBoxEnabled = false
local espColor = Color3.fromRGB(255, 255, 0) -- Amarelo Brasil inicial
local isGodMode = false

-- DataStoreService para salvar configurações
local DataStoreService = game:GetService("DataStoreService")
local settingsStore = DataStoreService:GetDataStore("BrasilMenuSettings")

-- GUI PRINCIPAL
local BrasilMenu = Instance.new("ScreenGui")
BrasilMenu.Name = "BrasilMenu"
BrasilMenu.ResetOnSpawn = false
BrasilMenu.Parent = PlayerGui

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 800, 0, 550) -- Tamanho ajustado para a imagem
MainFrame.Position = UDim2.new(0.5, -400, 0.5, -275)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Visible = true -- Começa visível para a tela de boas-vindas
MainFrame.Parent = BrasilMenu

local UICornerMain = Instance.new("UICorner")
UICornerMain.CornerRadius = UDim.new(0, 10)
UICornerMain.Parent = MainFrame

-- Top Bar (com o design da imagem)
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 50)
TopBar.Position = UDim2.new(0, 0, 0, 0)
TopBar.BackgroundColor3 = Color3.fromRGB(0, 150, 0) -- Verde Brasil
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopBarGradient = Instance.new("UIGradient")
TopBarGradient.Color = ColorSequence.new{ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 150, 0)), ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 100, 0))}
TopBarGradient.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Size = UDim2.new(0.5, 0, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 0) -- Amarelo Brasil
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 24
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Text = "BRASIL MENU"
TitleLabel.Parent = TopBar

local FlagImage = Instance.new("ImageLabel")
FlagImage.Name = "FlagImage"
FlagImage.Size = UDim2.new(0, 40, 0, 40)
FlagImage.Position = UDim2.new(0, 180, 0.5, -20)
FlagImage.BackgroundTransparency = 1
FlagImage.Image = "rbxassetid://553070660" -- Asset ID da bandeira do Brasil
FlagImage.Parent = TopBar

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -40, 0.5, -15)
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 20
CloseButton.Text = "X"
CloseButton.Parent = TopBar

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- Navigation Bar (Left Side)
local NavBar = Instance.new("Frame")
NavBar.Name = "NavBar"
NavBar.Size = UDim2.new(0, 180, 1, -50) -- Altura ajustada pela TopBar
NavBar.Position = UDim2.new(0, 0, 0, 50)
NavBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
NavBar.BorderSizePixel = 0
NavBar.Parent = MainFrame

local NavLayout = Instance.new("UIListLayout")
NavLayout.Name = "NavLayout"
NavLayout.FillDirection = Enum.FillDirection.Vertical
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
NavLayout.Padding = UDim.new(0, 5)
NavLayout.Parent = NavBar

-- Content Area
local ContentFrame = Instance.new("Frame")
ContentFrame.Name = "ContentFrame"
ContentFrame.Size = UDim2.new(1, -190, 1, -60)
ContentFrame.Position = UDim2.new(0, 185, 0, 55)
ContentFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ContentFrame.BorderSizePixel = 0
ContentFrame.Parent = MainFrame

-- Abas de Conteúdo (inicialmente invisíveis)
local tabs = {}
local function createTabFrame(name)
    local tab = Instance.new("ScrollingFrame")
    tab.Name = name .. "Tab"
    tab.Size = UDim2.new(1, 0, 1, 0)
    tab.BackgroundTransparency = 1
    tab.Visible = false
    tab.CanvasSize = UDim2.new(0, 0, 2, 0) -- Ajustável conforme o conteúdo
    tab.ScrollBarThickness = 6
    tab.Parent = ContentFrame
    
    local layout = Instance.new("UIListLayout")
    layout.Name = "ContentLayout"
    layout.FillDirection = Enum.FillDirection.Vertical
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.Padding = UDim.new(0, 10)
    layout.Parent = tab
    
    tabs[name] = tab
    return tab
end

local HomeTab = createTabFrame("Home")
local PlayerTab = createTabFrame("Jogador")
local MovementTab = createTabFrame("Movimento")
local VisualTab = createTabFrame("Visual")
local TeleportTab = createTabFrame("Teleporte")
local ConfigTab = createTabFrame("Config")

-- Função para criar botões de navegação com ícones
local currentActiveTab = HomeTab
local function createNavButton(name, iconAssetId, targetTab)
    local button = Instance.new("TextButton")
    button.Name = name .. "Button"
    button.Size = UDim2.new(0.9, 0, 0, 45)
    button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    button.TextColor3 = Color3.fromRGB(200, 200, 200)
    button.Font = Enum.Font.Gotham
    button.TextSize = 16
    button.TextXAlignment = Enum.TextXAlignment.Left
    button.Text = "  " .. name
    button.Parent = NavBar

    local iconLabel = Instance.new("ImageLabel")
    iconLabel.Name = "Icon"
    iconLabel.Size = UDim2.new(0, 24, 0, 24)
    iconLabel.Position = UDim2.new(0, 10, 0.5, -12)
    iconLabel.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Image = iconAssetId
    iconLabel.Parent = button

    local UICornerBtn = Instance.new("UICorner")
    UICornerBtn.CornerRadius = UDim.new(0, 6)
    UICornerBtn.Parent = button

    button.MouseButton1Click:Connect(function()
        if currentActiveTab then
            currentActiveTab.Visible = false
            -- Resetar cor do botão anterior
            for _, btn in pairs(NavBar:GetChildren()) do
                if btn:IsA("TextButton") and btn.Name == currentActiveTab.Name:gsub("Tab", "Button") then
                    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                    if btn:FindFirstChild("Icon") then btn.Icon.BackgroundColor3 = Color3.fromRGB(35, 35, 35) end
                end
            end
        end
        targetTab.Visible = true
        button.BackgroundColor3 = Color3.fromRGB(0, 180, 0) -- Cor de destaque verde
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        if button:FindFirstChild("Icon") then button.Icon.BackgroundColor3 = Color3.fromRGB(0, 180, 0) end
        currentActiveTab = targetTab
    end)
    return button
end

-- Ícones (substitua por Asset IDs reais do Roblox)
local ICON_HOME = "rbxassetid://6033008945" -- Exemplo
local ICON_PLAYER = "rbxassetid://6033008945" -- Exemplo
local ICON_MOVEMENT = "rbxassetid://6033008945" -- Exemplo
local ICON_VISUAL = "rbxassetid://6033008945" -- Exemplo
local ICON_TELEPORT = "rbxassetid://6033008945" -- Exemplo
local ICON_CONFIG = "rbxassetid://6033008945" -- Exemplo

createNavButton("HOME", ICON_HOME, HomeTab)
createNavButton("JOGADOR", ICON_PLAYER, PlayerTab)
createNavButton("MOVIMENTO", ICON_MOVEMENT, MovementTab)
createNavButton("VISUAL", ICON_VISUAL, VisualTab)
createNavButton("TELEPORTE", ICON_TELEPORT, TeleportTab)
createNavButton("CONFIGURAÇÕES", ICON_CONFIG, ConfigTab)

-- Ativar a aba Home por padrão e destacar seu botão
HomeTab.Visible = true
for _, btn in pairs(NavBar:GetChildren()) do
    if btn:IsA("TextButton") and btn.Name == "HomeButton" then
        btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        if btn:FindFirstChild("Icon") then btn.Icon.BackgroundColor3 = Color3.fromRGB(0, 180, 0) end
        break
    end
end

-- Dragging functionality for MainFrame
local dragging
local dragInput
local dragStart
local startPos

local function doDrag(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragInput = input
        dragStart = input.Position
        startPos = MainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        doDrag(input)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input == dragInput then
        dragging = false
    end
end)

-- Funções Auxiliares para UI (Toggles, Sliders, etc.)
local function createToggle(parent, text, callback, initialState)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.95, 0, 0, 40)
    frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.TextSize = 16
    label.Parent = frame
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 60, 0, 28)
    btn.Position = UDim2.new(0.95, -65, 0.5, -14)
    btn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    btn.Text = "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.Parent = frame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    local active = initialState or false
    
    local function updateButtonState()
        btn.Text = active and "ON" or "OFF"
        btn.BackgroundColor3 = active and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(255, 0, 0)
    end
    updateButtonState()

    btn.MouseButton1Click:Connect(function()
        active = not active
        updateButtonState()
        callback(active)
    end)
    return btn, function(state) active = state; updateButtonState() end
end

local function createSlider(parent, text, minValue, maxValue, defaultValue, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.95, 0, 0, 60)
    frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0.4, 0)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text .. ": " .. defaultValue
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.TextSize = 16
    label.Parent = frame
    
    local sliderBar = Instance.new("Frame")
    sliderBar.Size = UDim2.new(0.9, 0, 0, 10)
    sliderBar.Position = UDim2.new(0.05, 0, 0.6, 0)
    sliderBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    sliderBar.BorderSizePixel = 0
    sliderBar.Parent = frame
    
    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 5)
    sliderCorner.Parent = sliderBar
    
    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new(0, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBar
    
    local sliderHandle = Instance.new("Frame")
    sliderHandle.Size = UDim2.new(0, 15, 0, 15)
    sliderHandle.Position = UDim2.new(0, 0, 0.5, -7.5)
    sliderHandle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sliderHandle.BorderSizePixel = 0
    sliderHandle.Parent = sliderBar
    
    local handleCorner = Instance.new("UICorner")
    handleCorner.CornerRadius = UDim.new(0, 7)
    handleCorner.Parent = sliderHandle
    
    local currentValue = defaultValue
    local isDragging = false
    
    local function updateSlider(newVal)
        currentValue = math.clamp(newVal, minValue, maxValue)
        label.Text = text .. ": " .. math.floor(currentValue)
        local fillRatio = (currentValue - minValue) / (maxValue - minValue)
        sliderFill.Size = UDim2.new(fillRatio, 0, 1, 0)
        sliderHandle.Position = UDim2.new(fillRatio, -sliderHandle.Size.X.Offset / 2, 0.5, -7.5)
        callback(currentValue)
    end
    
    sliderHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local mousePos = UserInputService:GetMouseLocation()
            local barPos = sliderBar.AbsolutePosition
            local barSize = sliderBar.AbsoluteSize
            local relativeX = math.clamp((mousePos.X - barPos.X) / barSize.X, 0, 1)
            local newVal = minValue + (maxValue - minValue) * relativeX
            updateSlider(newVal)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
            isDragging = false
        end
    end)
    
    updateSlider(defaultValue)
    return sliderHandle, updateSlider
end

local function createButton(parent, text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.95, 0, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    btn.Parent = parent
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- HOME TAB
local WelcomeLabel = Instance.new("TextLabel")
WelcomeLabel.Size = UDim2.new(1, 0, 0, 50)
WelcomeLabel.BackgroundTransparency = 1
WelcomeLabel.Text = "BEM-VINDO AO BRASIL MENU!"
WelcomeLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
WelcomeLabel.Font = Enum.Font.GothamBold
WelcomeLabel.TextSize = 28
WelcomeLabel.Parent = HomeTab

local InfoFrame = Instance.new("Frame")
InfoFrame.Size = UDim2.new(0.95, 0, 0, 100)
InfoFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
InfoFrame.BorderSizePixel = 0
InfoFrame.Parent = HomeTab

local InfoLayout = Instance.new("UIListLayout")
InfoLayout.FillDirection = Enum.FillDirection.Horizontal
InfoLayout.Padding = UDim.new(0, 10)
InfoLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
InfoLayout.Parent = InfoFrame

local function createInfoDisplay(parent, icon, labelText, initialValue)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.2, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Parent = parent
    
    local iconLabel = Instance.new("ImageLabel")
    iconLabel.Size = UDim2.new(0, 30, 0, 30)
    iconLabel.Position = UDim2.new(0.5, -15, 0, 5)
    iconLabel.BackgroundTransparency = 1
    iconLabel.Image = icon
    iconLabel.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 20)
    label.Position = UDim2.new(0, 0, 0, 40)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 14
    label.Parent = frame
    
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Name = "ValueLabel"
    valueLabel.Size = UDim2.new(1, 0, 0, 20)
    valueLabel.Position = UDim2.new(0, 0, 0, 60)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = initialValue
    valueLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextSize = 16
    valueLabel.Parent = frame
    
    return valueLabel
end

local usernameLabel = createInfoDisplay(InfoFrame, "rbxassetid://6033008945", "USUÁRIO", LocalPlayer.Name)
local gameTimeLabel = createInfoDisplay(InfoFrame, "rbxassetid://6033008945", "TEMPO DE JOGO", "00:00:00")
local pingLabel = createInfoDisplay(InfoFrame, "rbxassetid://6033008945", "PING", "-- ms")
local fpsLabel = createInfoDisplay(InfoFrame, "rbxassetid://6033008945", "FPS", "--")

-- Atualização de FPS e Ping
RunService.Heartbeat:Connect(function()
    fpsLabel.Text = tostring(math.floor(1 / RunService.Heartbeat:Wait()))
    pingLabel.Text = tostring(math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue() * 1000)) .. " ms"
end)

-- Atualização de Tempo de Jogo
local startTime = tick()
RunService.Heartbeat:Connect(function()
    local elapsed = tick() - startTime
    local minutes = math.floor(elapsed / 60)
    local seconds = math.floor(elapsed % 60)
    gameTimeLabel.Text = string.format("%02i:%02i:%02i", math.floor(minutes / 60), minutes % 60, seconds)
end)

-- PLAYER TAB
createSlider(PlayerTab, "WalkSpeed", 0, 100, currentWalkSpeed, function(value)
    currentWalkSpeed = value
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = value
    end
end)

createSlider(PlayerTab, "JumpPower", 0, 200, 50, function(value)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character.Humanoid.JumpPower = value
    end
end)

createButton(PlayerTab, "Resetar Personagem", function()
    LocalPlayer:LoadCharacter()
end)

-- MOVEMENT TAB
local flyToggle, setFlyToggleState = createToggle(MovementTab, "Fly", function(active)
    isFlying = active
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")

    if active then
        hum.PlatformStand = true
        hum.Sit = true
        hum.WalkSpeed = 0
        hum.JumpPower = 0
        hum.AutoRotate = false
        
        local bv = Instance.new("BodyVelocity")
        bv.Name = "FlyBodyVelocity"
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Velocity = Vector3.new(0,0,0)
        bv.Parent = hrp
    else
        hum.PlatformStand = false
        hum.Sit = false
        hum.WalkSpeed = currentWalkSpeed
        hum.JumpPower = 50
        hum.AutoRotate = true
        if hrp and hrp:FindFirstChild("FlyBodyVelocity") then
            hrp.FlyBodyVelocity:Destroy()
        end
    end
end, isFlying)

-- Fly Movement Logic (andar no ar)
RunService.RenderStepped:Connect(function()
    if isFlying and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if not hum or not hrp then return end

        local moveVector = Vector3.new(0,0,0)

        -- Teclado
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVector += Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVector -= Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVector -= Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVector += Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveVector += Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveVector -= Vector3.new(0,1,0) end

        -- Mobile (simplificado, um joystick virtual seria ideal)
        if UserInputService.TouchEnabled then
            local touches = UserInputService:GetTouches()
            local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
            local touchSensitivity = 0.05

            for _, touch in ipairs(touches) do
                local relativePos = (touch.Position - screenCenter) / screenCenter.Magnitude
                moveVector += Camera.CFrame.LookVector * -relativePos.Y * touchSensitivity
                moveVector += Camera.CFrame.RightVector * relativePos.X * touchSensitivity

                if touch.Position.Y < screenCenter.Y * 0.5 then
                    moveVector += Vector3.new(0, 1, 0) * touchSensitivity
                elseif touch.Position.Y > screenCenter.Y * 1.5 then
                    moveVector -= Vector3.new(0, 1, 0) * touchSensitivity
                end
            end
        end

        if moveVector.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + moveVector.Unit * flySpeed * RunService.Heartbeat:Wait()
        end
    end
end)

createToggle(MovementTab, "No Clip", function(active)
    isNoClip = active
    if LocalPlayer.Character then
        for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.CanCollide = not active
            end
        end
    end
end, isNoClip)

createToggle(MovementTab, "Infinite Jump", function(active)
    isInfiniteJump = active
    if active then
        LocalPlayer.CharacterAdded:Connect(function(char)
            char:WaitForChild("Humanoid").Changed:Connect(function(prop)
                if prop == "FloorMaterial" and isInfiniteJump then
                    LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        end)
    end
end, isInfiniteJump)

createToggle(MovementTab, "Sprint", function(active)
    isSprint = active
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = active and 30 or currentWalkSpeed
    end
end, isSprint)

createButton(MovementTab, "Dash", function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local direction = hrp.CFrame.LookVector * 50 -- Dash de 50 studs
        hrp.CFrame = hrp.CFrame + direction
    end
end)

createToggle(MovementTab, "Auto Run", function(active)
    isAutoRun = active
    if active then
        -- Implementar lógica de auto run (ex: forçar movimento para frente)
    else
        -- Parar auto run
    end
end, isAutoRun)

-- VISUAL TAB
createToggle(VisualTab, "FullBright", function(active)
    isFullBright = active
    if active then
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.Brightness = 2
    else
        Lighting.Ambient = Color3.fromRGB(0, 0, 0)
        Lighting.Brightness = 1
    end
end, isFullBright)

createToggle(VisualTab, "Mostrar Nomes", function(active)
    isShowingNames = active
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local head = player.Character.Head
            local nameTag = head:FindFirstChild("NameTagBillboard")

            if active then
                if not nameTag then
                    nameTag = Instance.new("BillboardGui")
                    nameTag.Name = "NameTagBillboard"
                    nameTag.Size = UDim2.new(0, 150, 0, 30)
                    nameTag.Adornee = head
                    nameTag.AlwaysOnTop = true
                    nameTag.ExtentsOffset = Vector3.new(0, 1.5, 0)

                    local nameLabel = Instance.new("TextLabel")
                    nameLabel.Name = "PlayerNameLabel"
                    nameLabel.Size = UDim2.new(1, 0, 1, 0)
                    nameLabel.BackgroundTransparency = 1
                    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                    nameLabel.Font = Enum.Font.GothamBold
                    nameLabel.TextSize = 18
                    nameLabel.Text = player.Name
                    nameLabel.Parent = nameTag

                    nameTag.Parent = head
                else
                    nameTag.Enabled = true
                end
            else
                if nameTag then
                    nameTag.Enabled = false
                end
            end
        end
    end
end, isShowingNames)

createToggle(VisualTab, "Remover Neblina (No Fog)", function(active)
    isNoFog = active
    if active then
        Lighting.FogEnd = 100000 -- Remove a neblina
    else
        Lighting.FogEnd = 10000 -- Valor padrão do Roblox
    end
end, isNoFog)

createSlider(VisualTab, "Mudar FOV", 30, 120, currentFOV, function(value)
    currentFOV = value
    Camera.FieldOfView = value
end)

createToggle(VisualTab, "Zoom Livre", function(active)
    isZoomFree = active
    if active then
        LocalPlayer.CameraMinZoomDistance = 0.5
        LocalPlayer.CameraMaxZoomDistance = 1000
    else
        LocalPlayer.CameraMinZoomDistance = 0.5
        LocalPlayer.CameraMaxZoomDistance = 128
    end
end, isZoomFree)

-- ESP TAB (Re-implementado com base no design e funcionalidades)
local espHighlights = {}
local espLines = {}
local espBoxes = {}

local function createEspHighlight(target)
    local highlight = Instance.new("Highlight")
    highlight.Adornee = target
    highlight.FillColor = espColor
    highlight.OutlineColor = espColor
    highlight.FillTransparency = 0.5
    highlight.OutlineTransparency = 0
    highlight.Parent = target
    return highlight
end

local function createEspLine(target)
    local line = Instance.new("Part")
    line.Name = "EspLine"
    line.Anchored = true
    line.CanCollide = false
    line.Transparency = 0.5
    line.BrickColor = BrickColor.new(espColor)
    line.FormFactor = Enum.FormFactor.Custom
    line.Size = Vector3.new(0.1, 0.1, 0.1)
    line.Parent = workspace
    return line
end

local function createEspBox(target)
    local box = Instance.new("BoxHandleAdornment")
    box.Adornee = target
    box.Color = espColor
    box.AlwaysOnTop = true
    box.ZIndex = 10
    box.Parent = target
    return box
end

local function updateEspVisuals()
    for _, highlight in pairs(espHighlights) do highlight:Destroy() end
    for _, line in pairs(espLines) do line:Destroy() end
    for _, box in pairs(espBoxes) do box:Destroy() end
    espHighlights = {}
    espLines = {}
    espBoxes = {}

    if not isEspEnabled then return end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            local target = player.Character.HumanoidRootPart
            if target then
                local highlight = createEspHighlight(target)
                table.insert(espHighlights, highlight)

                if isEspLineEnabled then
                    local line = createEspLine(target)
                    table.insert(espLines, line)
                end

                if isEspBoxEnabled then
                    local box = createEspBox(target)
                    table.insert(espBoxes, box)
                end
            end
        end
    end
    -- Adicionar lógica para NPCs aqui, se necessário
end

local function updateEspLinesAndBoxesPositions()
    if not isEspEnabled then return end

    for i, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            local target = player.Character.HumanoidRootPart
            if target then
                if isEspLineEnabled and espLines[i] then
                    local line = espLines[i]
                    local origin = LocalPlayer.Character.HumanoidRootPart.Position
                    local destination = target.Position
                    local distance = (origin - destination).Magnitude
                    line.Size = Vector3.new(0.1, 0.1, distance)
                    line.CFrame = CFrame.new(origin, destination) * CFrame.new(0, 0, -distance / 2)
                end

                if isEspBoxEnabled and espBoxes[i] then
                    local box = espBoxes[i]
                    box.Adornee = target
                end
            end
        end
    end
end

createToggle(EspTab, "ESP Ativado", function(active)
    isEspEnabled = active
    updateEspVisuals()
end, isEspEnabled)

createToggle(EspTab, "Linhas ESP", function(active)
    isEspLineEnabled = active
    updateEspVisuals()
end, isEspLineEnabled)

createToggle(EspTab, "Caixas ESP", function(active)
    isEspBoxEnabled = active
    updateEspVisuals()
end, isEspBoxEnabled)

local EspColorButton = createButton(EspTab, "Cor ESP (Amarelo)", function()
    if espColor == Color3.fromRGB(255, 255, 0) then -- Amarelo
        espColor = Color3.fromRGB(0, 255, 0) -- Verde
        EspColorButton.Text = "Cor ESP (Verde)"
    elseif espColor == Color3.fromRGB(0, 255, 0) then -- Verde
        espColor = Color3.fromRGB(0, 0, 255) -- Azul
        EspColorButton.Text = "Cor ESP (Azul)"
    else -- Azul
        espColor = Color3.fromRGB(255, 255, 0) -- Amarelo
        EspColorButton.Text = "Cor ESP (Amarelo)"
    end
    updateEspVisuals()
end)

RunService.RenderStepped:Connect(updateEspLinesAndBoxesPositions)
Players.PlayerAdded:Connect(updateEspVisuals)
Players.PlayerRemoving:Connect(updateEspVisuals)

-- TELEPORT TAB
local function teleportToPosition(position)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(position)
    end
end

local function createTeleportButton(parent, text, position)
    return createButton(parent, text, function() teleportToPosition(position) end)
}

-- Exemplo de teleportes rápidos (ajuste as posições conforme seu mapa)
createTeleportButton(TeleportTab, "Spawn", Vector3.new(0, 10, 0))
createTeleportButton(TeleportTab, "Base", Vector3.new(100, 10, 0))
createTeleportButton(TeleportTab, "Cidade", Vector3.new(0, 10, 100))

-- Teleporte para Jogadores (Dropdown ou Input)
local PlayerTeleportFrame = Instance.new("Frame")
PlayerTeleportFrame.Size = UDim2.new(0.95, 0, 0, 80)
PlayerTeleportFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
PlayerTeleportFrame.BorderSizePixel = 0
PlayerTeleportFrame.Parent = TeleportTab

local PlayerTeleportLabel = Instance.new("TextLabel")
PlayerTeleportLabel.Size = UDim2.new(1, 0, 0, 20)
PlayerTeleportLabel.BackgroundTransparency = 1
PlayerTeleportLabel.Text = "Teleportar para Jogador:"
PlayerTeleportLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
PlayerTeleportLabel.TextXAlignment = Enum.TextXAlignment.Left
PlayerTeleportLabel.Font = Enum.Font.GothamBold
PlayerTeleportLabel.TextSize = 16
PlayerTeleportLabel.Parent = PlayerTeleportFrame

local PlayerNameInput = Instance.new("TextBox")
PlayerNameInput.Size = UDim2.new(0.7, 0, 0, 30)
PlayerNameInput.Position = UDim2.new(0.05, 0, 0, 30)
PlayerNameInput.PlaceholderText = "Nome do jogador..."
PlayerNameInput.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
PlayerNameInput.TextColor3 = Color3.fromRGB(255, 255, 255)
PlayerNameInput.Font = Enum.Font.Gotham
PlayerNameInput.TextSize = 14
PlayerNameInput.Parent = PlayerTeleportFrame

local TeleportPlayerBtn = Instance.new("TextButton")
TeleportPlayerBtn.Size = UDim2.new(0.2, 0, 0, 30)
TeleportPlayerBtn.Position = UDim2.new(0.8, 0, 0, 30)
TeleportPlayerBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
TeleportPlayerBtn.Text = "TP"
TeleportPlayerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
TeleportPlayerBtn.Font = Enum.Font.GothamBold
TeleportPlayerBtn.TextSize = 14
TeleportPlayerBtn.Parent = PlayerTeleportFrame

TeleportPlayerBtn.MouseButton1Click:Connect(function()
    local targetPlayerName = PlayerNameInput.Text
    local targetPlayer = Players:FindFirstChild(targetPlayerName)
    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
        teleportToPosition(targetPlayer.Character.HumanoidRootPart.Position + Vector3.new(0, 5, 0))
    else
        warn("Jogador " .. targetPlayerName .. " não encontrado ou sem personagem.")
    end
end)

-- CONFIG TAB
local function applyTheme(themeName)
    if themeName == "Light" then
        MainFrame.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        TopBar.BackgroundColor3 = Color3.fromRGB(220, 220, 220)
        TitleLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
        CloseButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        NavBar.BackgroundColor3 = Color3.fromRGB(180, 180, 180)
        ContentFrame.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        -- Atualizar cores de outros elementos (botões, sliders, etc.)
    elseif themeName == "Dark" then
        MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
        TopBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        CloseButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        NavBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        ContentFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        -- Atualizar cores de outros elementos
    elseif themeName == "Brasil" then
        MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
        TopBar.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
        CloseButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        NavBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        ContentFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        -- Atualizar cores de outros elementos para verde/amarelo
    end
end

createButton(ConfigTab, "Tema Claro", function() applyTheme("Light") end)
createButton(ConfigTab, "Tema Escuro", function() applyTheme("Dark") end)
createButton(ConfigTab, "Tema Brasil", function() applyTheme("Brasil") end)

createButton(ConfigTab, "Salvar Configurações", function()
    local settings = {
        isFlying = isFlying,
        flySpeed = flySpeed,
        currentWalkSpeed = currentWalkSpeed,
        isNoClip = isNoClip,
        isInfiniteJump = isInfiniteJump,
        isSprint = isSprint,
        isAutoRun = isAutoRun,
        isFullBright = isFullBright,
        isShowingNames = isShowingNames,
        isNoFog = isNoFog,
        currentFOV = currentFOV,
        isZoomFree = isZoomFree,
        isEspEnabled = isEspEnabled,
        isEspLineEnabled = isEspLineEnabled,
        isEspBoxEnabled = isEspBoxEnabled,
        espColor = {espColor.R, espColor.G, espColor.B},
        isGodMode = isGodMode,
    }
    pcall(function()
        settingsStore:SetAsync(LocalPlayer.UserId .. "_BrasilMenuSettings", settings)
        warn("Configurações salvas!")
    end)
end)

createButton(ConfigTab, "Carregar Configurações", function()
    pcall(function()
        local settings = settingsStore:GetAsync(LocalPlayer.UserId .. "_BrasilMenuSettings")
        if settings then
            isFlying = settings.isFlying
            setFlyToggleState(isFlying) -- Precisa de uma função para setar o estado do toggle
            flySpeed = settings.flySpeed
            currentWalkSpeed = settings.currentWalkSpeed
            isNoClip = settings.isNoClip
            -- setNoClipToggleState(isNoClip) -- E assim por diante para todos os toggles
            isInfiniteJump = settings.isInfiniteJump
            isSprint = settings.isSprint
            isAutoRun = settings.isAutoRun
            isFullBright = settings.isFullBright
            isShowingNames = settings.isShowingNames
            isNoFog = settings.isNoFog
            currentFOV = settings.currentFOV
            isZoomFree = settings.isZoomFree
            isEspEnabled = settings.isEspEnabled
            isEspLineEnabled = settings.isEspLineEnabled
            isEspBoxEnabled = settings.isEspBoxEnabled
            espColor = Color3.fromRGB(settings.espColor[1], settings.espColor[2], settings.espColor[3])
            isGodMode = settings.isGodMode
            warn("Configurações carregadas!")
            -- Chamar as funções de atualização para cada estado carregado
        else
            warn("Nenhuma configuração encontrada para carregar.")
        end
    end)
end)

-- Inicialização (aplica configurações salvas ou padrão)
-- loadSettings() -- Descomentar após implementar todas as funções de setar estado

print("Brasil Menu carregado com sucesso!")
