-- BRASIL MENU - PAINEL DE DESENVOLVEDOR
-- Desenvolvido para testes e desenvolvimento em projetos privados

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera

-- Variáveis de Estado
local isFlying = false
local flySpeed = 2
local currentSpeed = 16
local isNoClip = false
local isShowingNames = false
local isEspEnabled = false
local isEspLineEnabled = false
local isEspBoxEnabled = false
local espColor = Color3.fromRGB(255, 255, 0) -- Amarelo Brasil inicial

-- GUI PRINCIPAL
local BrasilMenu = Instance.new("ScreenGui")
BrasilMenu.Name = "BrasilMenu"
BrasilMenu.ResetOnSpawn = false
BrasilMenu.Parent = PlayerGui

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 650, 0, 450)
MainFrame.Position = UDim2.new(0.5, -325, 0.5, -225)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.Parent = BrasilMenu

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

-- BARRA SUPERIOR (TEMA BRASIL)
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 45)
TopBar.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 10)
TopCorner.Parent = TopBar

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "BRASIL MENU 🇧🇷"
Title.TextColor3 = Color3.fromRGB(255, 255, 0)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.Parent = TopBar

-- BARRA LATERAL DE NAVEGAÇÃO
local NavBar = Instance.new("Frame")
NavBar.Size = UDim2.new(0, 150, 1, -45)
NavBar.Position = UDim2.new(0, 0, 0, 45)
NavBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
NavBar.BorderSizePixel = 0
NavBar.Parent = MainFrame

local NavLayout = Instance.new("UIListLayout")
NavLayout.Padding = UDim.new(0, 5)
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
NavLayout.Parent = NavBar

-- ÁREA DE CONTEÚDO
local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -160, 1, -55)
ContentArea.Position = UDim2.new(0, 155, 0, 50)
ContentArea.BackgroundTransparency = 1
ContentArea.Parent = MainFrame

local function createTab(name)
    local tab = Instance.new("ScrollingFrame")
    tab.Name = name .. "Tab"
    tab.Size = UDim2.new(1, 0, 1, 0)
    tab.BackgroundTransparency = 1
    tab.Visible = false
    tab.CanvasSize = UDim2.new(0, 0, 2, 0)
    tab.ScrollBarThickness = 4
    tab.Parent = ContentArea
    
    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 10)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.Parent = tab
    
    return tab
end

-- ABAS
local HomeTab = createTab("Home")
local PlayerTab = createTab("Jogador")
local MovementTab = createTab("Movimento")
local VisualTab = createTab("Visual")
local TeleportTab = createTab("Teleporte")
local EspTab = createTab("ESP")
local ConfigTab = createTab("Config")

-- FUNÇÃO PARA CRIAR BOTÕES DE NAVEGAÇÃO
local currentTab = HomeTab
local function addNavButton(name, targetTab)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.Parent = NavBar
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        currentTab.Visible = false
        targetTab.Visible = true
        currentTab = targetTab
    end)
end

addNavButton("HOME", HomeTab)
addNavButton("JOGADOR", PlayerTab)
addNavButton("MOVIMENTO", MovementTab)
addNavButton("VISUAL", VisualTab)
addNavButton("TELEPORTE", TeleportTab)
addNavButton("ESP", EspTab)
addNavButton("CONFIG", ConfigTab)

HomeTab.Visible = true -- Começa na Home

-- FUNÇÕES DE MOVIMENTO (FLY CORRIGIDO)
local function toggleFly(state)
    isFlying = state
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    
    if isFlying then
        hum.PlatformStand = true
        local bv = Instance.new("BodyVelocity")
        bv.Name = "FlyVelocity"
        bv.Velocity = Vector3.new(0,0,0)
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Parent = hrp
    else
        hum.PlatformStand = false
        if hrp:FindFirstChild("FlyVelocity") then
            hrp.FlyVelocity:Destroy()
        end
    end
end

RunService.RenderStepped:Connect(function()
    if isFlying and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local moveDir = Vector3.new(0,0,0)
        
        -- Controles Teclado
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0,1,0) end
        
        if hrp:FindFirstChild("FlyVelocity") then
            hrp.FlyVelocity.Velocity = moveDir * (flySpeed * 50)
        end
    end
    
    -- NoClip
    if isNoClip and LocalPlayer.Character then
        for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
            if v:IsA("BasePart") and v.CanCollide then
                v.CanCollide = false
            end
        end
    end
end)

-- BOTÃO DE ABRIR/FECHAR
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 100, 0, 35)
ToggleBtn.Position = UDim2.new(0, 10, 0, 10)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
ToggleBtn.Text = "BRASIL MENU"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 0)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 12
ToggleBtn.Parent = BrasilMenu

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- COMPONENTES DE UI (TOGGLES E SLIDERS)
local function createToggle(parent, text, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.9, 0, 0, 40)
    frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    frame.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.Parent = frame
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 60, 0, 25)
    btn.Position = UDim2.new(0.95, -60, 0.5, -12)
    btn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    btn.Text = "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Parent = frame
    
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        btn.Text = active and "ON" or "OFF"
        btn.BackgroundColor3 = active and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(255, 0, 0)
        callback(active)
    end)
end

-- ADICIONANDO FUNÇÕES ÀS ABAS
createToggle(MovementTab, "Ativar Fly", function(v) toggleFly(v) end)
createToggle(MovementTab, "No Clip", function(v) isNoClip = v end)

createToggle(VisualTab, "FullBright", function(v)
    if v then
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.Brightness = 2
    else
        Lighting.Ambient = Color3.fromRGB(127, 127, 127)
        Lighting.Brightness = 1
    end
end)
